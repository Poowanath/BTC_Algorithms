services:
  - type: web
    name: btc-algorithms-bot
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn bot.app:app --host 0.0.0.0 --port ${PORT:-8000}
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
